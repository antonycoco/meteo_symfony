{% extends 'base.html.twig' %}

{% block title %}Billtain{% endblock %}

{% block body %}
    <h1>Billtain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billtain.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ billtain.description }}</td>
            </tr>
            <tr>
                <th>Tempirature</th>
                <td>{{ billtain.tempirature }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ billtain.date ? billtain.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('billtain_index') }}">back to list</a>

    <a href="{{ path('billtain_edit', {'id': billtain.id}) }}">edit</a>

    {{ include('billtain/_delete_form.html.twig') }}
{% endblock %}
