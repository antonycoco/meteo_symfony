{% extends 'base.html.twig' %}

{% block title %}Billtain index{% endblock %}

{% block body %}
    <h1>Billtain index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Tempirature</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for billtain in billtains %}
            <tr>
                <td>{{ billtain.id }}</td>
                <td>{{ billtain.description }}</td>
                <td>{{ billtain.tempirature }}</td>
                <td>{{ billtain.date ? billtain.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('billtain_show', {'id': billtain.id}) }}">show</a>
                    <a href="{{ path('billtain_edit', {'id': billtain.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('billtain_new') }}">Create new</a>
{% endblock %}
